[package]
name = "elixcee-types"
version = "0.1.0"
edition = "2024"
license = "MIT"
publish = false
description = "Shared value types for elixcee (Variant, ExcelError, CellContent, date-serial math) — internal, not independently versioned yet."
